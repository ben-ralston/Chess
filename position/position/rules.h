#ifndef RULES_H
#define RULES_H

#include "game_state.h"
#include "position.h"
#include "chess/piece.h"

//namespace rules {

//bool inCheck(bool white, const GameState &gameState);
//bool inCheck(bool white, int from[2], int to[2], const GameState &gameState);

//bool validBishopMove(int from[2], int to[2], const GameState &gameState);
//bool validKingMove(int from[2], int to[2], const GameState &gameState);
//bool validKnightMove(int from[2], int to[2], const GameState &gameState);
//bool validMove(int from[2], int to[2], bool white, const GameState &gameState, bool allowChecks = false);
//bool validPawnMove(int from[2], int to[2], const GameState &gameState);
//bool validQueenMove(int from[2], int to[2], const GameState &gameState);
//bool validRookMove(int from[2], int to[2], const GameState &gameState);
//}  // namespace rules

#endif // RULES_H
